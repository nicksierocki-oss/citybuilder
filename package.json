{
  "name": "gridline",
  "private": true,
  "type": "module",
  "description": "Gridline: a small browser city builder. No build step; package.json only holds the dev checks (npm run check).",
  "scripts": {
    "check": "npm run typecheck && npm test && npm run playtest && npm run smoke",
    "typecheck": "tsc -p .",
    "test": "node tools/test.js",
    "playtest": "node tools/playtest.js",
    "smoke": "node tools/smoke.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1",
    "typescript": "^5.9.3"
  }
}
